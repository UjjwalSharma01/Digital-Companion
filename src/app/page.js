import ChatWrapper from "../components/ClientWrapper";

export default function Home() {
  return (
    <div className="min-h-screen bg-[--background-gradient]">
      <ChatWrapper />
    </div>
  );
}
